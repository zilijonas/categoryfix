# `packages/config-typescript`

Shared TypeScript configuration package placeholder.

This package exists so future phases can keep app and package configs consistent while preserving simple `pnpm` workspaces.
