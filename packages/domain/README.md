# `packages/domain`

Pure deterministic domain logic for CategoryFix.

Planned contents:

- recommendation types
- confidence labels
- rule evaluation
- explanation payload builders
- apply and rollback payload planning

Rules:

- no React imports
- no Shopify SDK imports
- no database access
- deterministic logic first
